version: "3.8"

services:
  gradio-chat-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "7860:7860"
    environment:
      - PYTHONUNBUFFERED=1  # Direktes Loggen in Docker
    restart: unless-stopped
